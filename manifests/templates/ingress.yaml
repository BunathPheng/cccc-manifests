---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cccc                  # Ingress name
  labels:
    app: cccc
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "cccc.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: cccc               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
